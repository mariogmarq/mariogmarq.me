import { Options } from "$fresh/plugins/twind.ts";

export default {
  selfURL: import.meta.url,
  theme: {
    fontFamily: {
      "sans": ["Helvetica", "Arial", "sans-serif"],
    },
  },
} as Options;
